using System;

// Guarda os enumeradores de tipos, para facilitar encontrá-los.
public class Tipos
{
	// Tipos dos botões
	public enum Botao
	{
		Zero = 0, Positivo = 1, Negativo = 2, Multiplicador = 3
	}
}

public enum Mensagens {
	realizacao = 0, gradeCheia = 1, jogoZerado = 2
}

public enum MensagensImagens {
	mensagem = 0, aviso = 1, realizacao = 2
}

